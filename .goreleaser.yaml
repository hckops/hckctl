# see https://goreleaser.com
before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/main.go
    env:
      - CGO_ENABLED=0
    # https://pkg.go.dev/cmd/link
    ldflags:
      # remove symbol table and debug information
      - -s -w
      - -X github.com/hckops/hckctl/pkg/command.commit={{ .Version }}
      - -X github.com/hckops/hckctl/pkg/command.timestamp={{ .Date }}
    goos:
      - darwin
      - linux
      - windows
    ignore:
      - goos: darwin
        goarch: 386

archives:
  - name_template: >-
      {{ .ProjectName }}-
      {{- .Version }}-
      {{- .Os }}-
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format: tar.gz
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "{{ .ProjectName }}-{{ .Version }}-checksums.txt"
